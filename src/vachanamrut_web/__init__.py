"""Web-app concerns: durable storage, vouchers, and the shared question history.

Kept out of `vachanamrut_rag`, which is about the scripture and its retrieval,
and out of `api/`, where every module would be treated as its own endpoint.
"""
