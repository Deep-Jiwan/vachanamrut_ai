# Intermediate extraction output; rebuild with scripts/build_all.sh
data/cache/

# Derived at startup, or built only when an embedding backend is configured
data/corpus/bm25.pkl
data/corpus/vectors.json

__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/
.venv/

# Secrets — never commit
.env
.env.*
!.env.example
.vercel/

# Voucher codes and the secret that derives them — never commit
vouchers.txt

# Local SQLite database
*.db
*.db-wal
*.db-shm
